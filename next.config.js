// next.config.js

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images: {
      domains: ['i.imgur.com'],
    },
    // Add other configurations here
  };
  
  module.exports = nextConfig;
   